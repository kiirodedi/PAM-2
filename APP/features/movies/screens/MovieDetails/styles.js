import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontWeight: "bold",
        fontSize: 24,
        marginLeft: 10,
        marginBottom: 15,
        marginTop: 10,
    },
    sinopse: {
        fontSize: 16,
        marginLeft: 10,
        marginBottom: 15,
    },
    subtitle: {
        fontWeight: "bold",
        fontSize: 20,
        marginLeft: 10,
    },
});

export default styles;