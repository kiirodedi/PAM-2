import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 24,
    },
    sinopse: {
        fontSize: 16,
        marginTop: 10,
    },
});

export default styles;